# telechargement paquet via https + gestion de depot de logiciels plus recents afin d'assurer une stabilite + utilitaire dl fichier 
sudo apt-get install -y apt-transport-https software-properties-common wget

sudo apt-get update -y
sudo apt-get upgrade -y